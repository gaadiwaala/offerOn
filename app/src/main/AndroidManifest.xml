<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.offeron" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OfferOn" >
        <activity android:name=".fragments.play_bottom_frag" >
        </activity>
        <activity android:name=".fragments.chat_bottom_frag" />
        <activity android:name=".fragments.cartview_bottom_frag" />
        <activity android:name=".fragments.search_bottom_frag" />
        <activity android:name=".fragments.bottom_home_frag" />
        <activity android:name=".otp_activity" />
        <activity android:name=".signup_activity" />
        <activity android:name=".login_activity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".splash_screen" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>